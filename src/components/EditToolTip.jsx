import React from 'react';

const EditToolTip = (props) => (
    <div className={'text-sm p-1 bg-yellow-600 w-fit right-1 mb-1 font-semibold text-white'}>Editable cells turn yellow on mouse hover. Double tap on yellow cell to edit</div>
);

export default EditToolTip;